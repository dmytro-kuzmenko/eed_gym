# 08 — Add a scenario

Extend the scenario sampler in the environment (risk base-rate, noise, and thresholds).  
If you want custom stressors for OOD sweeps, edit the `STRESSORS` list in `scripts/st_eval.py`.
